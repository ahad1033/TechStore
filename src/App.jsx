// This file is not used since we're using React Router

import { ProductCategories } from "./components/sections";
import {  HomePage, ProductDetailPage } from "./pages";

// The main entry point is in main.jsx with RouterProvider
export default function App() {
  return <ProductDetailPage  />;
}
